import 'package:flutter/material.dart';

class AddRekapMedisPage extends StatelessWidget {
  const AddRekapMedisPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Rekap Medis'),
      ),
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Column(
          children: [
            // Add your form fields here
            Text(""),
          ],
        ),
      ),
    );
  }
}
